# Frontend Guide

The frontend is a local research workstation served by `serve.py`. It displays backend-generated evidence and does not calculate strategy logic in the browser.

## Start

Start a local server from the project root:

```bash
python serve.py --port 8766
```

Open:

```text
http://127.0.0.1:8766/frontend/
```

`serve.py` defaults to port `8766`. Use `--port` only when you need a custom local port.

## Pages

- 投研问答
- 每日报告
- ETF
- TL
- 可转债
- 标的档案
- 数据状态
- 策略参数
- 参数说明

## Data Source

The frontend reads:

```text
outputs/latest/dashboard.json
```

It also uses local API endpoints for refresh jobs, PDF export, chat, asset details, database status, and strategy parameter reads/writes.

## Actions

- `一键刷新`: calls `POST /api/refresh`, runs the daily workflow, then reloads `outputs/latest/dashboard.json`.
- `Export PDF`: calls `POST /api/export-pdf` and opens a Chinese-safe PDF generated by the local backend.
- `Open Report`: links to the generated Excel workbook.
- `Save Strategy Params`: writes `configs/strategy_params.json`; users must refresh data before new parameters affect reports.
- `AI 智能问答`: chat starts in deterministic rule mode. The frontend asks for confirmation before enabling LLM calls; signals and rankings still come from backend rules.
- `AI 模型`: reads and writes model choices in `configs/model_config.json`; users can enter an OpenAI key into a password field, save it to the git-ignored local `.env`, verify connectivity, refresh available models, and choose the chat model. The plaintext key is never returned to the browser or stored in model configuration.

The refresh monitor shows the current business stage. On failure it displays the failed stage, detected issue, impact, whether previous data remains available, and a stage-specific action hint. Raw stderr remains in the refresh job record for technical troubleshooting; the normal page prioritizes user-readable guidance.

The AI model panel displays both the routine fast model (`economy_model`) and the deep-analysis model (`chat.primary_model`). Simple rule questions do not call either model.

The current chat page is backed by the v2 research runtime: deterministic Router, read-only tools, evidence accuracy review, optional ReAct supervisor, output guardrail, and trace persistence. The browser never receives database credentials or unrestricted tool access.

## Missing Excel Behavior

Frontend refresh follows the same degradation rule as the CLI:

- If ETF, TL, or convertible-bond Excel is missing individually, refresh still starts.
- The ingestion layer creates empty module frames and records data quality warnings.
- The missing module displays `unavailable` or `data_unavailable`; other modules continue.
- Only when all configured core source Excel files are missing does refresh return an immediate error.

## Visual Direction

- high information density
- restrained neutral palette
- clear risk/status colors
- table-first investment workflow
- no marketing landing page
- no decorative background effects
# ETF 策略切换

操作路径：`策略参数 → ETF策略 → 选择策略 → 保存 → 刷新数据`。

“已保存，待刷新后生效”表示配置已保存，但当前页面仍是上一次成功生成的结果。刷新成功后，ETF 表格同时显示“中期趋势状态”和“短期入场状态”；刷新失败时保留上一次成功结果。

当前默认 ETF 策略是 `trend_pullback_v2`。如果切换到 `legacy_v1`，v2 专属的“中期趋势、短期入场”列会隐藏；切回 v2 并刷新成功后重新显示。

刷新结果采用原子切换：页面不会读到一半旧数据、一半新数据或半写 JSON。
