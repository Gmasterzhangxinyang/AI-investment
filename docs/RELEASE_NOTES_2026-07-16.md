# 2026-07-16 文档一致性版本

## 当前策略口径

- 明确 ETF 当前默认是 `trend_pullback_v2`，原策略仅作为可切换插件与历史对照。
- 重新整理中期趋势、密切观察、过热保护、趋势确认日、突破/回踩确认和持仓退出规则。
- 明确可转债当前是 `legacy_v1` 基础决策层加 `dynamic_v2` 四项辅助，动态辅助不改变基础分、资格和排名。
- 修正产品说明中的 ETF MA5 平仓量能旧值 1.5，当前配置为 1.2。

## AI 问答

- 补齐当前三档运行路径、ReAct 主 Agent、只读工具列表、证据准确性审查、输出保护和降级行为。
- 补齐 ETF 单标的 30 日、多标的 2 至 10 只、双策略 400 日、TL 30 日和可转债前 30 只的数据范围。
- 明确多 ETF 当前状态使用确定性批量工具，开启 AI 也不允许漏标的或改写状态。

## 文档治理

- 新增 `docs/CURRENT_SYSTEM.md` 作为当前运行事实入口。
- 重写 ETF 模型、策略逻辑和产品使用说明，移除把原策略写成当前默认、把历史诊断写成完整回测等旧口径。
- 所有历史 plans/specs 增加“历史实施记录”标记，防止将早期 rollout 假设误认为当前配置。
- 新增自动测试，将当前配置、主说明、Agent 数据范围和历史文档标记纳入持续检查。
